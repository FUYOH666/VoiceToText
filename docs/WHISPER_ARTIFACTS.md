# Whisper: хвостовые «галлюцинации» и встроенный фильтр

## Почему это появляется

Whisper (и совместимые сервисы) иногда дописывает в конец транскрипта типичные для видео/субтитров фразы — даже когда речи не было или сигнал уже затих. Это не баг клиента VTT: текст приходит уже в ответе ASR. Параметры вроде `no_speech_threshold` на стороне Mac **не** переписывают ответ удалённого HTTP-сервиса.

## Что делает приложение

Перед автовставкой (`paste`) строка проходит **`strip_trailing_whisper_artifacts`** (`src/vtt2/text/whisper_artifacts.py`):

- обрабатывается **только хвост** (последняя непустая строка целиком или суффикс с безопасной границей перед фразой);
- списки фраз вшиты в код (RU и EN), основаны на публичных наблюдениях и дополняются по мере типичных хвостов (полный актуальный перечень — в `src/vtt2/text/whisper_artifacts.py`);
- длинные шаблоны проверяются раньше коротких.

При срабатывании в лог пишется краткое сообщение (сколько символов снято), без обязательного дампа полного текста.

## Настройка (`config.yaml`)

В секции `text_processing`:

| Параметр | По умолчанию | Смысл |
|----------|---------------|--------|
| `strip_whisper_tail_artifacts` | `true` | Включить снятие хвостов |
| `whisper_artifact_languages` | `["ru", "en"]` | Какие наборы фраз использовать |

Чтобы полностью отключить фильтр:

```yaml
text_processing:
  strip_whisper_tail_artifacts: false
```

Только английский список:

```yaml
text_processing:
  whisper_artifact_languages:
    - en
```

## Ограничения и компромиссы

- Если вы **намеренно** диктуете в самом конце фразу из списка (например «Продолжение следует»), она может быть удалена. Отключите фильтр или переформулируйте фразу.
- Фильтр не заменяет качественную настройку сервера ASR; он лишь убирает частые шаблоны перед вставкой.

## Ссылки

- Сборник типичных RU-хвостов (gist): [whisper-hallucinations-ru](https://gist.github.com/waveletdeboshir/8bf52f04bf78018194f25b2390c08309)
- Обсуждения вокруг hallucinations в openai/whisper: см. issues репозитория [openai/whisper](https://github.com/openai/whisper/issues) (поиск по «hallucination», «subtitles»).
