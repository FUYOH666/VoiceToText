# VTT2 — active hardware/profile (see config/profiles/)
# Override: VTT2_PROFILE=mac-m1-remote  or  --profile mac-m1-remote
# Secrets (ASR URL): .env.local — copy from .env.example

active_profile: mac-m1-remote
