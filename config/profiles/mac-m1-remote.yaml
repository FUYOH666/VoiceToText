# MacBook M1 — thin client, inference on private ASR server (Tailscale)
# Set LOCAL_AI_ASR_BASE_URL in .env.local (not committed)

transcription:
  engine: remote_asr

performance:
  memory_limit_mb: 2048
