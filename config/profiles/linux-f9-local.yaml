# Linux F9 client — ASR on same host (phase A3: clients/linux)
# Schema: F9 (asr.*), not VTT2 transcription.*

asr:
  base_url: "http://127.0.0.1:8001"
  transcription_endpoint: "/v1/audio/transcriptions"
  timeout: 30
  language: null
  response_format: "json"

hotkey:
  key: "f9"
