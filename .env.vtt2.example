# Copy to .env.vtt2 (gitignored) for launchd / service installs
# Prefer .env.local for interactive runs — both are loaded if present

VTT2_PROFILE=mac-m1-remote
LOCAL_AI_ASR_BASE_URL=http://YOUR_TAILSCALE_HOST:8001
LOCAL_AI_ASR_TIMEOUT=60
