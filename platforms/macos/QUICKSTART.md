# VTTv2 - Быстрый старт

## ✅ Что нужно установить

1. ✅ Клонировать репозиторий
2. ✅ Скомпилировать whisper.cpp
3. ✅ Скачать модель ggml-large-v3.bin
4. ✅ Установить Python зависимости

## 🚀 Быстрая установка

```bash
# 1. Клонирование репозитория
git clone git@github.com:FUYOH666/VoiceToText-MACos.git
cd VoiceToText-MACos

# 2. Клонирование и компиляция whisper.cpp
git clone https://github.com/ggerganov/whisper.cpp.git
cd whisper.cpp
mkdir build && cd build
cmake .. -DBUILD_SHARED_LIBS=ON
make -j8
cd ../..

# 3. Скачивание модели (2.9 GB)
mkdir -p models
cd models
curl -L "https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-large-v3.bin" -o ggml-large-v3.bin
cd ..

# 4. Установка Python зависимостей
python3.12 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt
```

## 🚀 Запуск

### Проверка системы (Health Check)

```bash
cd VoiceToText-MACos
source venv/bin/activate
python src/main.py --health
```

**Ожидаемый результат:**
```
=== Результаты Health Check ===
config: ✅
permissions_mic: ❌ (или ✅ после настройки)
permissions_accessibility: ✅
whisper_cpp: ✅
```

Если видите предупреждения о разрешениях - это нормально для первого запуска.

### Запуск приложения

**Полная команда:**

```bash
cd VoiceToText-MACos
source venv/bin/activate
python src/main.py
```

**Или пошагово:**

```bash
cd VoiceToText-MACos
source venv/bin/activate
python src/main.py
```

**Логи сохраняются в:** `./logs/vtt2.log`

Для просмотра логов в реальном времени:
```bash
tail -f logs/vtt2.log
```

## ⚙️ Настройка разрешений macOS

**ВАЖНО:** Перед запуском нужно настроить разрешения:

1. **Микрофон:**
   - Системные настройки > Конфиденциальность > Микрофон
   - Добавьте Terminal (или приложение, которое запускает VTTv2)

2. **Accessibility:**
   - Системные настройки > Конфиденциальность > Управление компьютером
   - Добавьте Terminal (или приложение, которое запускает VTTv2)

## 📝 Использование

1. Запустите приложение (`python src/main.py`)
2. Нажмите **Option+Space** для начала записи
3. Говорите в микрофон
4. Нажмите **Option+Space** еще раз для остановки и транскрипции
5. Текст автоматически вставится в место курсора

## 🎯 Иконки в menu bar

- 🎤 - Готов к записи
- 🔴 - Идет запись

## 🔧 Конфигурация

Основные параметры в `config.yaml`:

- `transcription.whisper_cpp.binary_path` - путь к whisper-cli (по умолчанию: `./whisper.cpp/build/bin/whisper-cli`)
- `transcription.whisper_cpp.model_path` - путь к модели (по умолчанию: `./models/ggml-large-v3.bin`)
- `transcription.whisper_cpp.no_speech_threshold` - порог чувствительности (рекомендуется: `0.3` для длинных записей)
- `ui.hotkey` - горячие клавиши (по умолчанию: `"option+space"`)

### Для длинных записей (15-30 минут)

Рекомендуется установить в `config.yaml`:

```yaml
transcription:
  whisper_cpp:
    no_speech_threshold: 0.3  # Более чувствительная транскрипция
```

## ⚠️ Важно

- Core ML и Metal автоматически используются whisper-cli если доступны
- Модель Large v3 обеспечивает максимальную точность
- Все проверки выполняются при старте (fail fast)
- Приложение работает полностью офлайн после установки
- **Поддержка длинных записей:** Оптимизировано для записей до 30 минут
- **Надежность:** Гарантированная целостность данных без артефактов
- **Диагностика:** Проверяйте логи при проблемах с транскрипцией

## 📚 Полная документация

См. [README.md](README.md) для полной документации и решения проблем.
