# VTTv2 - Voice-to-Text для macOS

<div align="center">

![Python](https://img.shields.io/badge/python-3.12+-blue.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)
![macOS](https://img.shields.io/badge/macOS-11.0+-lightgrey.svg)
![Platform](https://img.shields.io/badge/platform-Apple%20Silicon-orange.svg)

🎤 **Бесплатное локальное приложение для преобразования речи в текст на macOS**

Высокопроизводительное приложение для преобразования речи в текст с использованием whisper.cpp и Core ML для максимальной скорости на Apple Silicon (M1/M2/M3/M4).

[🚀 Быстрый старт](#-быстрый-старт) • [📖 Документация](#-документация) • [💡 Возможности](#-возможности) • [🤝 Вклад](#-вклад-в-проект)

</div>

## Краткое описание

VTTv2 - это бесплатное локальное приложение для macOS, которое преобразует речь в текст с использованием передовых технологий ИИ. Приложение оптимизировано для Apple Silicon и обеспечивает высокую точность транскрипции без необходимости в интернет-соединении.

## Проблемы, которые решает

- **Отсутствие приватных решений**: Большинство сервисов voice-to-text отправляют данные в облако
- **Высокая стоимость**: Платные решения требуют подписки от $10-30/месяц
- **Зависимость от интернета**: Невозможность работать офлайн
- **Низкая производительность на Mac**: Отсутствие оптимизации для Apple Silicon
- **Ограниченная кастомизация**: Закрытые решения без возможности настройки

## 🚀 Почему VTTv2?

### Сравнение с платными аналогами

| Особенность | VTTv2 (этот проект) | Платные решения (SuperWhisper и др.) |
|------------|---------------------|-------------------------------------|
| **Стоимость** | ✅ Бесплатно | ❌ Подписка от $10-30/мес |
| **Локальная обработка** | ✅ 100% локально | ⚠️ Часто облачные API |
| **Конфиденциальность** | ✅ Данные не покидают ваш Mac | ❌ Данные отправляются на сервер |
| **Офлайн работа** | ✅ Полностью офлайн | ❌ Требует интернет |
| **Кастомизация** | ✅ Полный контроль | ❌ Ограниченные настройки |
| **Модель** | ✅ Whisper Large v3 | ⚠️ Обычно Medium/Small |
| **Исходный код** | ✅ Открытый | ❌ Закрытый |

### Преимущества VTTv2

- 🆓 **Бесплатно навсегда** - никаких подписок и ограничений
- 🔒 **100% приватность** - все данные обрабатываются локально на вашем Mac
- ⚡ **Быстро** - оптимизация для Apple Silicon с Core ML и Metal
- 🎯 **Точность** - модель Whisper Large v3 для максимального качества
- 🔧 **Гибкость** - полный контроль над настройками и конфигурацией
- 📱 **Интеграция** - автовставка текста в место курсора

## 📋 Требования

- macOS (рекомендуется Apple Silicon: M1/M2/M3/M4)
- Python 3.12+
- Xcode Command Line Tools (`xcode-select --install`)
- Разрешения: Микрофон, Accessibility (управление компьютером)

## 🛠️ Установка

### 1. Клонирование репозитория

```bash
git clone git@github.com:FUYOH666/VoiceToText-MACos.git
cd VoiceToText-MACos
```

### 2. Установка whisper.cpp

```bash
# Клонирование whisper.cpp
git clone https://github.com/ggerganov/whisper.cpp.git

# Компиляция с поддержкой Core ML и Metal
cd whisper.cpp
mkdir build && cd build
cmake .. -DBUILD_SHARED_LIBS=ON
make -j8
cd ../..
```

**Результат:** `whisper.cpp/build/bin/whisper-cli` должен быть исполняемым

### 3. Скачивание модели

Модель Whisper Large v3 (2.9 GB) нужно скачать вручную:

```bash
# Создайте директорию models если её нет
mkdir -p models

# Скачайте модель из Hugging Face
cd models
curl -L "https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-large-v3.bin" -o ggml-large-v3.bin
cd ..
```

**Альтернативно:** См. инструкции в `models/README.md`

### 4. Установка Python зависимостей

```bash
# Создайте виртуальное окружение
python3.12 -m venv venv

# Активация venv
source venv/bin/activate

# Обновление pip
pip install --upgrade pip

# Установка зависимостей
pip install -r requirements.txt
```

### 5. Настройка разрешений macOS

**ВАЖНО:** Перед запуском нужно настроить разрешения:

1. **Микрофон:**
   - Системные настройки > Конфиденциальность > Микрофон
   - Добавьте Terminal (или приложение, которое запускает VTTv2)

2. **Accessibility (Управление компьютером):**
   - Системные настройки > Конфиденциальность > Управление компьютером
   - Добавьте Terminal (или приложение, которое запускает VTTv2)

## 🚀 Запуск

### Основной запуск

**Полная команда запуска:**

```bash
cd VoiceToText-MACos
source venv/bin/activate
python src/main.py
```

**Или пошагово:**

```bash
# 1. Перейти в директорию проекта
cd VoiceToText-MACos

# 2. Активировать виртуальное окружение
source venv/bin/activate

# 3. Запустить приложение
python src/main.py
```

Приложение появится в виде иконки 🎤 в строке меню macOS.

**Логи сохраняются в:** `./logs/vtt2.log` (создается автоматически)

**Просмотр логов:**

```bash
# Просмотр последних 100 строк
tail -100 logs/vtt2.log

# Просмотр в реальном времени
tail -f logs/vtt2.log

# Поиск ошибок
grep -E "(ERROR|WARNING)" logs/vtt2.log

# Поиск информации о записи
grep -E "(Запись|Транскрипция|аудио)" logs/vtt2.log
```

**Для детальной диагностики включите DEBUG логирование в `config.yaml`:**
```yaml
logging:
  level: DEBUG  # Вместо INFO
```

### Health Check

Проверка системы перед запуском:

```bash
cd VoiceToText-MACos
source venv/bin/activate
python src/main.py --health
```

Проверяет:
- ✅ Конфигурацию
- ✅ Разрешения (микрофон, Accessibility)
- ✅ Наличие whisper.cpp бинарника
- ✅ Наличие модели
- ✅ Инициализацию компонентов

## 💡 Использование

1. **Запуск приложения:** 
   ```bash
   cd VoiceToText-MACos
   source venv/bin/activate
   python src/main.py
   ```
2. **Горячие клавиши:** `Option+Space` для включения/выключения записи
3. **Иконка в menu bar:**
   - 🎤 - Готов к записи
   - 🔴 - Идет запись
4. **Автовставка:** Текст автоматически вставляется в место курсора после транскрипции
5. **Логи:** Все события записываются в `logs/vtt2.log` для диагностики

### Пример использования

1. Откройте любой текстовый редактор (TextEdit, Notes, и т.д.)
2. Нажмите `Option+Space` - иконка изменится на 🔴
3. Говорите в микрофон
4. Нажмите `Option+Space` еще раз - запись остановится
5. Через несколько секунд транскрибированный текст автоматически появится в месте курсора

## ⚙️ Конфигурация

Основные параметры в `config.yaml`:

### Параметры транскрипции

- `transcription.whisper_cpp.binary_path` - путь к бинарнику whisper.cpp (по умолчанию: `./whisper.cpp/build/bin/whisper-cli`)
- `transcription.whisper_cpp.model_path` - путь к модели (по умолчанию: `./models/ggml-large-v3.bin`)
- `transcription.whisper_cpp.use_coreml` - использование Core ML (рекомендуется: `true` для Apple Silicon)
- `transcription.whisper_cpp.use_metal` - использование Metal (рекомендуется: `true` для GPU ускорения)
- `transcription.whisper_cpp.threads` - количество потоков (8 для M4 Max, 4 для M1/M2)
- `transcription.whisper_cpp.language` - язык транскрипции (`ru` для русского, `en` для английского)
- `transcription.whisper_cpp.no_speech_threshold` - порог для определения "нет речи" (0.0-1.0)
  - **Рекомендуется:** `0.3` для длинных записей (до 2 часов) и тихой речи
  - **По умолчанию:** `0.3` (было 0.6)
  - Меньше значение = более чувствительная транскрипция
- `transcription.whisper_cpp.compression_ratio_threshold` - порог компрессии для фильтрации повторов (по умолчанию: `2.4`)

### Параметры аудио

- `audio.sample_rate` - частота дискретизации (по умолчанию: `16000`)
- `audio.channels` - количество каналов (по умолчанию: `1` - моно)
- `audio.chunk_size` - размер чанка для записи (по умолчанию: `1024`)
- `audio.max_recording_duration` - максимальная длительность записи в секундах (по умолчанию: `3600` - 1 час)

### Параметры UI

- `ui.hotkey` - горячие клавиши (по умолчанию: `"option+space"`)
- `ui.auto_paste_enabled` - автовставка текста (по умолчанию: `true`)
- `ui.auto_paste_method` - метод автовставки (`cgevent` или `clipboard`)

### Рекомендации для длинных записей (до 2 часов)

Для оптимальной транскрипции длинных записей рекомендуется:

```yaml
transcription:
  whisper_cpp:
    no_speech_threshold: 0.3  # Более чувствительная транскрипция
    threads: 8                 # Максимум потоков для M4 Max
```

Также убедитесь, что:
- Достаточно свободного места на диске (минимум 500MB)
- Микрофон настроен на оптимальную громкость
- Нет фонового шума, который может мешать транскрипции

## 🔧 Возможности

- ✅ **Высокая точность** - модель Whisper Large v3
- ✅ **Быстрая транскрипция** - оптимизация для Apple Silicon
- ✅ **Автовставка текста** - автоматическая вставка в место курсора
- ✅ **Глобальные горячие клавиши** - работа из любого приложения
- ✅ **Локальная обработка** - все данные остаются на вашем Mac
- ✅ **Настраиваемые параметры** - полный контроль через config.yaml
- ✅ **Поддержка русского языка** - оптимизация для русской речи
- ✅ **Длинные записи** - поддержка записей до 2 часов без потери данных
- ✅ **Надежность** - гарантированная целостность данных без артефактов
- ✅ **Диагностика** - детальное логирование для отладки проблем

## ❓ Частые проблемы

### 1. whisper.cpp не найден

**Ошибка:** `❌ Бинарник whisper.cpp не найден`

**Решение:**
- Убедитесь, что whisper.cpp скомпилирован: `whisper.cpp/build/bin/whisper-cli`
- Проверьте путь в `config.yaml`: `transcription.whisper_cpp.binary_path`
- Используйте абсолютный путь или путь относительно корня проекта

### 2. Модель не найдена

**Ошибка:** `❌ Модель не найдена`

**Решение:**
- Скачайте `ggml-large-v3.bin` в директорию `models/` (см. раздел "Установка")
- Проверьте путь в `config.yaml`: `transcription.whisper_cpp.model_path`
- Убедитесь, что размер файла ~2.9 GB

### 3. Разрешение на микрофон отклонено

**Ошибка:** `❌ Разрешение на микрофон отклонено`

**Решение:**
- Системные настройки > Конфиденциальность > Микрофон
- Добавьте Terminal или приложение в список разрешенных
- Перезапустите приложение

### 4. Разрешение Accessibility не предоставлено

**Ошибка:** `❌ Разрешение Accessibility не предоставлено`

**Решение:**
- Системные настройки > Конфиденциальность > Управление компьютером
- Добавьте Terminal или приложение в список разрешенных
- Перезапустите приложение

### 5. Автовставка не работает

**Симптомы:** Текст транскрибируется, но не вставляется

**Решение:**
- Проверьте разрешение Accessibility (см. выше)
- Проверьте настройку `ui.auto_paste_enabled: true` в `config.yaml`
- Попробуйте изменить `ui.auto_paste_method` на `"clipboard"` (текст будет скопирован в буфер обмена)
- Проверьте логи приложения для деталей

### 6. Ошибка транскрипции

**Симптомы:** Пустой результат или ошибка при транскрипции

**Решение:**
- Проверьте логи приложения
- Убедитесь, что аудио записывается (проверьте разрешения)
- Проверьте, что модель загружается корректно
- Попробуйте запустить whisper.cpp вручную с тестовым WAV файлом

### 7. Короткий результат транскрипции для длинных записей

**Симптомы:** Для записи 3-5 минут получено очень мало текста (например, 1500 символов вместо ожидаемых 5000+)

**Решение:**
- Проверьте логи в файле `logs/vtt2.log`:
  ```bash
  tail -100 logs/vtt2.log
  ```
- Проверьте уровень аудио сигнала в логах (должен быть > 0.01)
- Уменьшите `no_speech_threshold` до `0.2-0.3` в `config.yaml`
- Увеличьте громкость микрофона в системных настройках
- Проверьте логи на предупреждения о тихом аудио или пропущенных сегментах
- Убедитесь, что нет длительных пауз тишины в записи

### 8. Потеря данных или артефакты в длинных записях

**Симптомы:** Дублирование текста, пропуски в середине записи, зацикливание

**Решение:**
- Проверьте логи в файле `logs/vtt2.log`:
  ```bash
  tail -200 logs/vtt2.log | grep -E "(ERROR|WARNING|Запись|Транскрипция)"
  ```
- Убедитесь, что используется последняя версия кода (исправления применены)
- Проверьте логи на предупреждения о переполнении очереди
- Увеличьте `audio.chunk_size` до `2048` для более стабильной записи
- Убедитесь, что достаточно свободной памяти (рекомендуется 8GB+)
- Для детальной диагностики включите DEBUG логирование в `config.yaml`:
  ```yaml
  logging:
    level: DEBUG
  ```

## 📁 Архитектура

```
VTTv2/
├── src/
│   ├── main.py              # Главное приложение (rumps App)
│   ├── audio/               # Запись и обработка аудио
│   ├── transcription/       # Транскрипция через whisper.cpp
│   ├── system/              # Системная интеграция (разрешения, горячие клавиши, вставка текста)
│   ├── config/              # Загрузка и валидация конфигурации
│   └── utils/               # Утилиты (логирование)
├── whisper.cpp/             # whisper.cpp (клонировать отдельно)
├── models/                  # Модели (ggml-large-v3.bin) - скачать отдельно
├── config.yaml              # Конфигурация
├── requirements.txt         # Зависимости Python
└── README.md               # Этот файл
```

## 🛠️ Технологии

- **whisper.cpp** - транскрипция речи (Core ML + Metal ускорение)
- **Python 3.12** - основной язык разработки
- **rumps** - macOS menu bar приложение
- **PyObjC** - интеграция с macOS API
- **sounddevice** - запись аудио с микрофона
- **pydantic** - валидация конфигурации

## 📝 Примечания

- Приложение оптимизировано для Apple Silicon (M1/M2/M3/M4) с использованием Neural Engine и Metal
- Модель Large v3 обеспечивает максимальную точность транскрипции
- Все проверки выполняются при старте (fail fast подход)
- Автовставка текста работает через CGEvent (требует разрешения Accessibility)
- Приложение работает полностью офлайн - интернет не требуется после установки
- **Поддержка длинных записей:** Система оптимизирована для записей до 2 часов без потери данных
- **Надежность:** Гарантированная целостность данных без артефактов и потерь
- **Долгосрочная стабильность:** Автоматическая очистка памяти, готовность к работе неделями без перезапуска
- **Production-ready:** Удалены все тестовые компоненты, оптимизировано для production использования

## 📖 Документация

- [QUICKSTART.md](QUICKSTART.md) - Быстрый старт
- [CONTRIBUTING.md](CONTRIBUTING.md) - Вклад в проект
- [SECURITY.md](SECURITY.md) - Политика безопасности
- [CHANGELOG.md](CHANGELOG.md) - История изменений

## 🤝 Вклад в проект

Мы приветствуем вклад в развитие проекта! Если у вас есть идеи или исправления:

1. Fork репозитория
2. Создайте ветку для вашей функции (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для деталей.

## 🔗 Ссылки

- **GitHub репозиторий:** https://github.com/FUYOH666/VoiceToText-MACos
- **whisper.cpp:** https://github.com/ggerganov/whisper.cpp
- **Whisper модели:** https://huggingface.co/ggerganov/whisper.cpp

## 🤝 Вклад в проект

Мы приветствуем вклад от сообщества! См. [CONTRIBUTING.md](CONTRIBUTING.md) для деталей.

### Как помочь:

- ⭐ Поставьте звезду на GitHub
- 🐛 Сообщите об ошибках через Issues
- 💡 Предложите улучшения
- 🔧 Отправьте Pull Request с исправлениями
- 📢 Поделитесь проектом с друзьями

---

**Сделано с ❤️ для сообщества macOS разработчиков**

Если проект вам помог - поставьте ⭐ на GitHub!

## Контакты

**Автор:** FUYOH666

**Для коммерческих запросов:**
- 🌐 **Website:** [scanovich.ai](https://scanovich.ai)